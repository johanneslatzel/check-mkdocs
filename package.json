{
    "name": "@johannes.latzel/check-mkdocs",
    "version": "0.1.0",
    "description": "CLI tool and library to statically validate repository documentation (nav, orphans, internal links)",
    "license": "MIT",
    "files": [
        "dist/"
    ],
    "author": "Johannes B. Latzel",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/johanneslatzel/check-mkdocs.git"
    },
    "bugs": {
        "url": "https://github.com/johanneslatzel/check-mkdocs/issues"
    },
    "homepage": "https://github.com/johanneslatzel/check-mkdocs#readme",
    "keywords": [
        "docs",
        "mkdocs",
        "markdown",
        "lint",
        "cli"
    ],
    "engines": {
        "node": ">=20.9"
    },
    "publishConfig": {
        "access": "public"
    },
    "type": "module",
    "bin": {
        "check-mkdocs": "./dist/cli.js"
    },
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "scripts": {
        "prebuild": "rm -rf dist/",
        "prepack": "npm run build",
        "build": "tsc -p tsconfig.build.json",
        "verify": "tsc --noEmit && eslint . --max-warnings 0 && vitest run --coverage",
        "audit": "npm audit --audit-level=high",
        "lint": "eslint . --max-warnings 0",
        "format": "prettier --write 'docs/**/*.md' 'src/**/*.ts' 'tests/**/*.ts'",
        "docs": "docker run --rm -it -p 8000:8000 -v $PWD:/docs squidfunk/mkdocs-material",
        "test": "vitest",
        "test:coverage": "vitest run --coverage",
        "clean:pack": "rm -rf node_modules dist/ package-lock.json && npm cache clean --force && npm install && npm run verify && npm run build && npm pack",
        "clean:install": "rm -rf node_modules dist/ package-lock.json && npm cache clean --force && npm install && npm run verify",
        "prepare": "husky"
    },
    "lint-staged": {
        "src/**/*.ts": [
            "prettier --write",
            "eslint --max-warnings 0 --no-warn-ignored",
            "vitest related --run"
        ]
    },
    "dependencies": {
        "js-yaml": "^5.4.2"
    },
    "devDependencies": {
        "@eslint/js": "^10.0.1",
        "@types/js-yaml": "^4.0.9",
        "@types/node": "^26.6.2",
        "@typescript-eslint/eslint-plugin": "^8.68.0",
        "@typescript-eslint/parser": "^8.68.0",
        "@vitest/coverage-v8": "^5.0.1",
        "eslint": "^10.9.1",
        "eslint-config-prettier": "^10.1.2",
        "eslint-plugin-n": "^18.3.0",
        "husky": "^9.1.7",
        "lint-staged": "^17.4.1",
        "prettier": "^3.5.3",
        "typescript": "^6.0.3",
        "vitest": "^5.0.1"
    }
}
